public class Task_4 {
    public static void main(String[] args) {
        // 4.Зачем нужна область видимости переменной? Было бы удобно, если б переменная, объявленная хоть где,
        // была доступна в любом другом месте?


        // Слишком большое количество переменных сложно обрабатывать человеку (как вариант проблема нейминга)
        // Так же необходимо экономить стек! Все переменные области видимости загружаются в стек, который значительно меньше чем ОП.
    }
}
